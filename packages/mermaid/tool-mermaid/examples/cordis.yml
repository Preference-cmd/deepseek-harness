# Opt-in patch overlay adding the mermaid diagram tool to a base-backed profile.
# This file is a PATCH OVERLAY (not a tree): apply it after the profile's own
# cordis.patch.yml, so the row below lands beside the shipped tool rows.
# `dsh --profile headless --patch packages/mermaid/tool-mermaid/examples/cordis.yml`
- id: tool-mermaid
  name: '@deepseek-ai/dsh-tool-mermaid'
